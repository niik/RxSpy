<UserControl x:Class="RxSpy.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:rx="clr-namespace:ReactiveUI.Xaml;assembly=ReactiveUI.Xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ctl="clr-namespace:RxSpy.Views.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="600">
    <Grid Margin="5">
        
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition/>
        </Grid.RowDefinitions>
        
        <GroupBox Header="Tracked observables" Margin="10">
            <rx:ViewModelViewHost x:Name="observablesGrid" Margin="10" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch" />
        </GroupBox>

        <GroupBox Header="Details" Margin="10" Grid.Row="2" >
            <rx:ViewModelViewHost Margin="10" x:Name="detailsView" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch"  />
        </GroupBox>

    </Grid>
</UserControl>
