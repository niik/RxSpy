<UserControl x:Class="RxSpy.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:rx="clr-namespace:ReactiveUI.Xaml;assembly=ReactiveUI.Xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ctl="clr-namespace:RxSpy.Views.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="600">
    <Grid>
        
        <Grid.RowDefinitions>
            <RowDefinition Height="70*" />
            <RowDefinition Height="30*" />
        </Grid.RowDefinitions>
        
        <rx:ViewModelViewHost x:Name="observablesGrid" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch" />

        <rx:ViewModelViewHost Grid.Row="2" x:Name="detailsView" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch"  />

    </Grid>
</UserControl>
